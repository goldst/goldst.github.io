{
  "devDependencies": {
    "babel-cli": "*",
    "babel-core": "*",
    "babel-preset-env": "*",
    "chai": "*",
    "eslint": "*",
    "htmllint": "*",
    "htmllint-cli": "*",
    "jsdoc-to-markdown": "^5.0.0",
    "jsdoctest": "^1.7.1",
    "maid": "*",
    "mocha": "*",
    "stylelint": "^10.0.1",
    "stylelint-order": "*"
  },
  "dependencies": {
    "postcss-syntax": "^0.28.0"
  },
  "babel": {
    "extends": "./config/babel.json"
  },
  "scripts": {
    "test": "# this project uses maid for automatization. Run 'npx maid --help' for further information."
  }
}
